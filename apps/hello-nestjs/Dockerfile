# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @hello/hello-nestjs`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @hello/hello-nestjs`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @hello/hello-nestjs && \
          adduser --system -G @hello/hello-nestjs @hello/hello-nestjs

COPY apps/hello-nestjs/dist @hello/hello-nestjs/
RUN chown -R @hello/hello-nestjs:@hello/hello-nestjs .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @hello/hello-nestjs --omit=dev -f install

CMD [ "node", "@hello/hello-nestjs" ]
